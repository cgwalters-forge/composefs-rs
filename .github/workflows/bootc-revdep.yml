name: bootc Reverse Dependency CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always
  LIBVIRT_DEFAULT_URI: "qemu:///session"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  bootc-test:
    name: bootc with local composefs-rs (${{ matrix.name }})
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    # The ostree backend test can't pass until composefs-capi keeps ostree's
    # object paths in the images it writes (convert.rs); run it for
    # information until then.
    continue-on-error: ${{ matrix.recipe == 'test-ostree' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: composefs backend
            recipe: test
          # ostree's composefs support running on our libcomposefs
          - name: ostree backend
            recipe: test-ostree

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Setup
        uses: bootc-dev/actions/bootc-ubuntu-setup@main
        with:
          libvirt: true

      - name: Install tmt
        run: |
          sudo apt-get install -y pipx
          pipx install tmt[provision]
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Build and test bootc with local composefs-rs
        run: just bootc/${{ matrix.recipe }}
